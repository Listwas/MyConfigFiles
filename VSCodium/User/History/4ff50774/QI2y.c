#include <stdio.h>

int main() 
{
    int celcius, fahr;
    int step, lower, upper; 

    lower = 0;
    upper = 30;
    step = 20;
    celcius = lower;

    while (celcius <= upper) {
        fahr = (fahr - 32) * 5/9;

    }
}